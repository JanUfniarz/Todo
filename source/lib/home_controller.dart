import 'package:todo_app/data_manager.dart';
import 'package:todo_app/todo_model.dart';

class HomeController {
  DataManager dataManager;

  HomeController(this.dataManager);

  void merge(TodoModel todo)
}